node_modules/
.claude/
dist/
builds/
.worktrees/
.claude/worktrees/
*.log
.playwright-mcp/
*.png
# ...except world portrait assets, which are legitimately .png (e.g. an
# official portrait only published as PNG) and must ship with the world.
!worlds/*/assets/portraits/*.png
.agent/
.vscode/
