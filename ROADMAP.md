# Roadmap

Future work, none of this is implemented. See `CHANGELOG.md` for what's shipped and `README.md` for known v1 deviations.

- remove isochrones
- remove duplicate maps, after max-borders
- simplify variables and make an "standard engine" app for creating map apps easily integrated a database as JSON file or no-SQL or SQL
